import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0069aa',
    },
    secondary: {
      main: '#214F66',
    },
    success: {
      main: '#7AC142',
    },
  },
});

export default theme;
