import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0069aa',
    },
    secondary: {
      main: '#224c65',
    },
    success: {
      main: '#7AC142',
    },
  },
});

export default theme;
